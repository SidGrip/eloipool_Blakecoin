# Build artifacts
__pycache__/
*.pyc
*.pyo

# Runtime artifacts (the bundle is a deploy template, not a running pool)
*.log
*.stdout
*.stderr
share-logfile
filename.log

# AGPL bypass marker — created by build-bundle.sh and deploy.sh as a runtime
# artifact so eloipool can boot on non-git deploys. Must NOT be committed.
# See deploy-bundle/scripts/build-bundle.sh for the full explanation.
.I_swear_that_I_am_Luke_Dashjr
**/.I_swear_that_I_am_Luke_Dashjr
